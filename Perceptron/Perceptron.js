class Perceptron {

    constructor() {
        this.weights = [];

        for (let i = 0; i < this.weights.length; i++) {
            this.weights[i] = Math.random(-1, 1);
        }
    }

    guess(inputs) {
        let sum = 0;
    }
}